AFW — Enter CTA Patch
=======================
This patch changes the homepage hero to a **single CTA** that says **"enter"** and links to **/healing**.
It also tracks the event as **cta_enter**.

Install
-------
1) Place this file at: `src/pages/Home.tsx` (overwrite).
2) Commit & redeploy on Vercel (Clear build cache).

Notes
-----
- Sign in / Sign up / Sign out remain in the top-right as before.
- Protected areas and journal privacy remain unchanged.
