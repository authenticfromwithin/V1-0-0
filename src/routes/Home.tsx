import React from 'react';
export default function Home(){
  return (<section className="card">
    <h1>Authentic From Within — v1 foundation</h1>
    <p className="hint">This is the app shell. Assets & advanced modules attach when placed under <code>public/assets</code>.</p>
  </section>);
}